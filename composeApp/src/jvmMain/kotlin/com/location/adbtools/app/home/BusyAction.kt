package com.location.adbtools.app.home

/**
 * 当前界面的忙碌动作类型。
 */
enum class BusyAction {
    Connecting,
    Disconnecting,
    Refreshing,
    BrowsingFiles,
    Deleting,
    Installing,
    Pulling,
    Pushing,
}
